<!-- _队列集实验_改进程序框架 -->
游戏机案例三个功能：红外遥控器控制，旋转编码器控制，陀螺仪控制，
三个硬件如果创建三个任务各自读写队列，会消耗很多内存，
因此考虑使用队列集方式；

解耦功能，三个中断函数只将自身硬件数据写入一个input任务，
该任务负责解析硬件数据，写入挡球板队列；

input任务读取多个硬件数据可以使用轮询方式，此时超时时间要设为0，
这种方式占用CPU资源多，较低效，
可以使用队列集，队列集存放各队列的句柄。
任务流程：
1.创建队列ABC
2.创建队列集
3.将ABC句柄放入队列集
4.input任务读队列集获取句柄，获取数据