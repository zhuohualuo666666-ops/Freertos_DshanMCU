<!-- 资源管理_互斥操作的本质 -->

### 屏蔽中断：
·任务中：
屏蔽中断函数
taskENTER_CRITICAL()
恢复中断函数
taskEXIT_CRITICAL()

·中断中：
屏蔽中断函数
uxSavedInterruptStatus = taskENTER_CRITICAL_FROM_ISR();
恢复中断函数:
taskEXIT_CRITICAL_FROM_ISR( uxSavedInterruptStatus )

其中uxSavedInterruptStatus用来保存当前中断的状态（是否被屏蔽）

### 屏蔽调度器：
暂停调度器：
void vTaskSuspendAll(void)
恢复调度器：
BaseType_t xTaskResumeAll(void)

