<!-- 软件定时器的本质 -->
###
创建定时器时会创建一个结构体，该结构体包含:
1.flag:表示该定时器是一次性或周期性
2.period：周期
3.函数
4.参数
5.链表项（用于将定时器放入某个链表）

###
定时器使用方法：
一.在中断中使用：
1.判断定时器链表中有无超时的定时器
2.判断为有调用其函数

该方法时候不复杂的中断功能处理

二. 通知/唤醒任务
1.判断定时器链表中有无超时的定时器
2.判断为有写队列唤醒定时器任务
3.定时器任务读、处理队列，进行启动、停止、复位等操作

###
定时器任务优先级要设置较高，或者将定时器任务设置为事件驱动