<!-- 优化系统_打印所有人物的栈信息 -->

voidvTaskList( signed char *pcWriteBuffer )

该函数可以获取任务统计信息,其中参数为自己设定的一个环形缓冲区

void vApplicationIdleHook( void )
{
	vTaskList(pcWriteBuffer);
    rintf("%s\n\r", pcWriteBuffer);
}

可以在空闲任务的hook函数中编写任务信息打印功能，
可打印出任务名称，任务状态，任务剩余空闲栈，任务序号信息